# Render Blueprint - defines the backend service
services:
  - type: web
    name: crate-digging-api
    runtime: python
    pythonVersion: "3.10"
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
